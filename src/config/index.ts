export const config = {
  shopify: {
    accessToken: import.meta.env.VITE_APP_SHOPIFY_ACCESS_TOKEN,
    domain: import.meta.env.VITE_APP_SHOPIFY_DOMAIN,
    apiVersion: import.meta.env.VITE_APP_SHOPIFY_API_VERSION,
  },
};
